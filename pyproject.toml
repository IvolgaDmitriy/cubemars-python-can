[project]
authors = [{name = "Ivolga Dmitriy", email = "ivolga.dmitriy@yandex.ru"}]
dependencies = ["bitstring>=4.3.1,<5"]
name = "cubemars-python-can"
requires-python = ">= 3.9"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.pypi-dependencies]
cubemars_python_can = { path = ".", editable = true }

[tool.pixi.tasks]

[tool.pixi.dependencies]
loguru = ">=0.7.3,<0.8"
